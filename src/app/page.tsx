import Image from "next/image";
import style from "@/app/Styles/Home.module.css";
export default function Home() {
  return (
    
      <div className={`${style.Home} h-screen `}>
        <h1 className={`${style.mainHead}`}>SAVIOUR</h1>
      </div>
  );
}
